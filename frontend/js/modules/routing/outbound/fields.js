/**
 * Outbound Form Fields Module (Schema-Driven)
 * Fields are dynamically rendered and managed by sentinel-core schema.
 */
import { getCurrentOutboundValues, setCurrentOutboundValues } from "./modal_manager.js";

export function updateOutboundFormFields() {
    // Dynamic schema handles field toggling automatically via evaluateShowIfCondition
}
